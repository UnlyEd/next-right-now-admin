import React from 'react';

const CustomerBulkActionButtons = (props) => {
  console.debug('CustomerBulkActionButtons.props', props);
  return (
    <>
      Bulk delete has been disabled
    </>
  );
};

export default CustomerBulkActionButtons;
